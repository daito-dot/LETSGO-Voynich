# Hypothesis ledger addendum — Issue #64 / #58C

Date: 2026-09-01

Exact method and thresholds are controlled by `experiments/occupancy-graph-residual/PLAN_A.md`. Exact first-reveal values and provenance are controlled by `REPORT_A.md` and `first-reveal/`.

## Program-level object

This phase studies internal construction of **space-delimited Voynich tokens** under the frozen 12-slot representation. It is not sentence syntax and does not assume visible spaces are proven natural-language word boundaries.

## H58C-EXIST — residual token-construction graph exists beyond lower-order occupancy architecture

Prediction:

After calibrating all 66 conditional occupancy edges against an independently estimated line-local slot-prevalence null, the pooled residual graph will exceed an independent test-null ensemble and reproduce across physical leaves.

Frozen gates:

- `p_exist_ALL <= .01`;
- `W_ALL >= .50`.

Observed:

- real `E_ALL = 3.2315507868`;
- test-null median `0.9900432214`;
- test-null 95th percentile `1.1342638078`;
- test-null maximum `1.2831839812`;
- `p_exist_ALL = 1/1001 = 0.000999000999`;
- `W_ALL = 0.9447148364`.

Status: **STRONGLY SUPPORTED**.

This rejects, under the frozen representation/null, the explanation that the broad token-internal association graph is only a by-product of line-local slot prevalence / lower-order occupancy architecture.

## H58C-STRATA — residual interaction exists in all planned manuscript strata

Observed:

All seven source-grounded groups passed the family-wise maxT residual-existence gate with `p_E,maxT = 1/1001` and `W = 0.8968–0.9551`.

Status: **SUPPORTED**.

The residual construction signal is not confined to a single Currier class, section, or line position.

## H58C-SHARED — one exactly stable residual graph across all tested strata

Frozen overall class for exact shared stability required stable register/section and stable position families.

Observed:

Stable residual contrasts:

- section B vs H within Currier B;
- section B vs S within Currier B;
- section H vs S within Currier B;
- line interior vs final.

Related but modulated:

- Currier A vs B within Herbal;
- line initial vs final;
- line initial vs interior.

Frozen family classes:

- `REGISTER/SECTION RESIDUAL MODULATION`;
- `LINE-POSITION RESIDUAL MODULATION`.

Frozen overall classification:

> **`RESIDUAL GRAPH EXISTS WITH STRATUM MODULATION`**

Status: **EXACT UNIFORMITY NOT SUPPORTED; SHARED CORE WITH MODULATION SUPPORTED**.

## H58C-MULTI — multiple / materially different token grammars are required

The frozen `DIFFERENT_RESIDUAL_OR_MIXTURE` gate required supported residual existence plus either residual `R < .40` or a directional held-out transfer below `.30`.

Observed:

- no planned contrast met the difference gate;
- all seven residual similarities were family-wise exceptional against the independent test-null maxT distribution;
- real residual correlations ranged from `0.6247` to `0.8848`.

Status: **NOT SUPPORTED**.

The detected modulation is not currently strong enough to justify wholly separate token-generation grammars.

## Retained current model

The strongest supported description is:

> **A broad, reproducible token-internal residual construction system is shared across the tested manuscript strata, with measurable modulation concentrated especially in Currier A/B within Herbal and line-initial tokens.**

This is a surface-generation constraint, not a semantic or cipher interpretation.

## Parser sensitivity

The non-promoting parser-`max` analysis retained strong pooled residual energy (`3.1178578405`), high pooled reliability (`0.9532616582`) and the same qualitative topology: strongest agreement among Currier-B sections and interior/final positions, with weaker Currier A/B and line-initial relations.

This supports representation robustness within the existing parser ambiguity family but is not an independent transcription replication.

## New follow-up hypothesis generated after #58C

The next separately preregistered hypothesis should test **representation/transcription invariance**:

> The #58C residual-existence result and its shared-core-plus-modulation geometry survive a materially independent Voynich transcription/reading without retuning the slot grammar or selecting edges after observing the replication.

Scientific reason:

#58C now supplies a strong enough structural constraint that the main alternative to address before inverse-model use is transcription/parser dependence.

Required discipline:

1. audit an independent transcription and alignable physical-leaf population before target scoring;
2. freeze how its glyph inventory maps into the already established 12-slot parser, or document exactly why an outcome-independent compatibility layer is needed;
3. retain all 66 edges;
4. freeze replication statistics and agreement thresholds before revealing the independent-transcription residual graph;
5. distinguish replication of **residual existence** from replication of the exact modulation pattern;
6. do not use ZL3b edge identities or magnitudes to tune the independent mapping;
7. only after representation/transcription invariance is supported use the stable residual constraints prospectively to restrict reversible generative/inverse models.

## Interpretation boundary

Do not infer from #58C:

- that spaces are linguistic words;
- sentence grammar;
- slot meanings;
- a plaintext alphabet or cipher table;
- a specific historical generator;
- semantic presence/absence;
- decipherment.

## Disposition

- residual graph existence beyond lower-order occupancy: **STRONGLY SUPPORTED**;
- residual existence in all planned strata: **SUPPORTED**;
- exact invariant residual graph: **NOT SUPPORTED**;
- shared residual core with modulation: **SUPPORTED**;
- multiple materially separate residual grammars: **NOT SUPPORTED**;
- next frontier: **independent-transcription / representation invariance before inverse-model use**.