# Hypothesis Ledger Addendum — Issue #94 / Issue #88 Phase 2A

Date: 2026-09-06
Authority: `experiments/predictive-information/phase2a/REPORT.md`

## P94-1 — the Phase-1 long-history gain mainly requires actual long-range order

**State before Phase 2A:** LIVE.

**Result:** **REJECTED as the dominant explanation under the tested controls.**

The strong order-free A5 `LOCAL_PLUS_PREFIX-CV` model recovers `0.044306 bit/token` beyond LOCAL40 in 5/5 folds, compared with the entire Phase-1 LOCAL40→ORDERED128 increment of `0.046927 bit/token`.

Thus most of the apparent long-history predictive benefit can be obtained from the accumulated causal-prefix token-family inventory without preserving actual long-range order.

This does not establish what drives that inventory.

## P94-2 — causal prefix inventory carries predictive information beyond <=40 local history

**State:** **SUPPORTED.**

`G_prefix_cond = LOCAL40 - LOCAL_PLUS_PREFIX-CV`:

- mean `+0.044306 bit/token`;
- positive 5/5 folds;
- nested-selected `rho=.20-.21`.

The information source is slow/prefix-scale under the tested representation, but it is not yet identified as semantic topic, register, section, scribe or any other historical variable.

## P94-3 — actual source-to-lag association contains no information once causal prefix inventory is supplied

**State:** **REJECTED under the tested controls, with a small-effect caveat.**

Two prospective order controls both pass:

- `ORDERED128` beats `LOCAL_PLUS_PREFIX-CV` by only `+0.002621 bit/token`, positive 4/5 folds;
- `ORDERED128` beats calibrated matched RANDOM-LAG by `+0.019764 bit/token`, positive 5/5 folds.

Therefore an order/lag-sensitive residual survives. Its magnitude is control-dependent and is very small against the strongest hybrid, so this does not justify describing long-range order as the main mechanism.

## P94-4 — the Phase-1 H=ALL result establishes literal leaf-long memory

**State:** **FURTHER WEAKENED / NOT LICENSED.**

Phase 2A shows that most of the H=ALL benefit is reproduced by an order-free causal-prefix inventory. A literal long-memory interpretation is therefore unnecessary for the dominant effect.

The residual ordered effect still requires localization before any historical-memory interpretation.

## P94-5 — latent-state fitting is now licensed

**State:** **REJECTED.**

Observable and directly controllable predictive sources remain unresolved:

- causal-prefix inventory;
- residual order/lag association;
- line/paragraph position, already shown by Phase 1;
- independently authoritative manuscript strata and transport remain untested in this attribution framework.

The next frontier is Phase 2B observable-state / scale attribution, followed by transport, not a rich hidden-state model.

## Program consequence

The working decomposition is now:

1. compact token-internal V2 construction;
2. <=40 local edit-1 recency;
3. a substantial order-free causal-prefix inventory effect;
4. a smaller surviving order/lag-sensitive residual;
5. additional observable line/paragraph-position information.

The next question is where components 3–5 live across document geometry and manuscript strata, and whether their parameters transport.