# Hypothesis ledger addendum — Issue #121

Date: 2026-09-06
Parent: Issue #88 / Issue #118

| Hypothesis / question | Status | Evidence / consequence |
|---|---|---|
| Issue #118 cross-boundary residual is caused by RESET and CONT selecting different byte n-gram orders | **REJECTED** | L1 forced both experts to `k=2, alpha=.01`; `G_edge=+0.0106322 bit/token`, positive `5/5` |
| Immediate raw-surface context across adjacent visible units contains reproducible information beyond corrected B3 and same-order emission complement | **SUPPORTED — SMALL** | same-order L1 passes in all five folds; MIX_CONT2 `9.4866634` vs MIX_RESET2 `9.4972957` bits/token |
| The surviving same-order residual behaves like broad multi-line / long hidden byte memory | **NOT SUPPORTED BY L1 LOCALIZATION** | predeclared diagnostics: SAME_LINE total `+254.565 bits`; cross-line same-item `-1.940`; cross-item same-document `+0.095`; cross-document same-leaf `-0.313` |
| The residual is line-local | **CANDIDATE — NEXT CONFIRMATORY TEST** | descriptive L1 localization strongly suggests it, but this was not the primary L1 gate; must be tested prospectively by resetting CONT2 at source-line boundaries |
| Paragraph ENTRY / line FIRST causally explains the residual | **UNRESOLVED** | ENTRY/FIRST has larger descriptive mean (`~0.0193`) than BODY/MIDDLE (`~0.0083–0.0087`), but no state-conditioned confirmatory model has been run |
| A rich latent state is now directly interpretable | **NOT YET** | residual is only `~0.0106 bit/token` after same-order control and appears topologically narrow; line/regime/state localization remains required |

## Working update

The strongest economical interpretation after L1 is now:

> Visible certain-space units are bounded construction episodes. A compact internal grammar generates each unit; nearby edit-near families and slower prefix inventories modulate selection. In addition, a very small immediate surface dependency passes from one unit to the next, and the first prospective localization shows that this dependency is concentrated among adjacent units on the same source line rather than across line breaks.

This is a structural predictive statement only. It does not identify meaning, plaintext, language, cipher family, historical production mechanism or latent semantics.
