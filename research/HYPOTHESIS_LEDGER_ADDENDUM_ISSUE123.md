# Hypothesis ledger addendum — Issue #123

Date: 2026-09-06
Parent: Issue #88 / Issue #118 / Issue #121

| Hypothesis / question | Status | Evidence / consequence |
|---|---|---|
| The fixed-k2 boundary-edge residual requires carrying context across source-line breaks | **REJECTED** | `G_beyond_line = -0.0185292 bit/token`, positive `0/5`; LEAFCONT2 is worse than LINECONT2 in every outer fold |
| Carrying immediate surface context within a source line is sufficient to reproduce the residual | **SUPPORTED** | `G_line = +0.0291614 bit/token`, positive `5/5`; frozen L2 class `LINE-LOCAL CONTEXT CAPTURES RESIDUAL; NO ROBUST BEYOND-LINE GAIN` |
| The L1 SAME_LINE concentration was only a descriptive subgroup artifact | **REJECTED under sequential confirmatory L2** | prospectively inserted line resets improve the fixed-k2 challenger in all five folds while exact Issue #121 parent regression is preserved |
| The remaining residual behaves like generic long hidden sequence memory | **NOT SUPPORTED** | extending identical k=2 context across line breaks is predictively harmful; the useful scope is narrower, within-line adjacency |
| Source line is therefore a natural-language sentence / semantic clause | **NOT ESTABLISHED** | L2 identifies predictive reset topology only; no semantic or linguistic boundary test was performed |
| A compact explicit previous-terminal → next-initial edge can absorb the line-local gain | **OPEN — NEXT HIGH-VALUE GATE** | k=2 LINECONT2 exposes token-specific previous-unit information principally at the next token's first byte; test prospectively before latent state |
| Line-local edge strength is invariant across Currier A/B | **OPEN** | Currier transport is an authorized observable regime question but has not yet been tested for the L2 edge mechanism |
| A rich latent state is now directly interpretable | **NOT YET** | residual is localized to a narrow observable topology; compact edge and regime/state controls remain before latent-state interpretation |

## Working update

The economical structural hypothesis after L2 is:

> Visible certain-space units are bounded construction episodes generated by a compact internal grammar. Nearby units are coupled at several weak scales: a broadly transportable edit-near recurrence mechanism, slower paragraph/prefix inventory, and a very small immediate raw-surface edge dependency. The latter is now prospectively localized to adjacent units on the same source line; the same short context should reset at a line break.

This remains a structural predictive model, not a decipherment or historical mechanism claim.
