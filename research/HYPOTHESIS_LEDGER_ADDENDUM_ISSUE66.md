# Hypothesis ledger addendum — Issue #66 / #58D

Date: 2026-09-01

Exact method and thresholds are controlled by `experiments/occupancy-graph-independent-transcription/SOURCE_AUDIT_PLAN.md` and `PLAN_A.md`. Exact first-reveal values/provenance are controlled by `REPORT_A.md` and `first-reveal/`.

## Program-level object

This phase studies internal construction of **space-delimited Voynich tokens** under the established 12-slot representation. It is not sentence syntax and does not assume visible spaces are proven natural-language word boundaries.

The replication tests an independent manuscript reading lineage inside a common EVA/IVTFF representational framework.

## H58D-SOURCE — IT2a is an adequate preregistered independent-reading target

Prediction / authorization requirement:

A source/population-only audit, performed before pair/residual scoring, will show that IT2a is unambiguously the Takahashi `EvaT` source, substantially overlaps the frozen #58C manuscript population, and is accepted directly enough by the unchanged 12-slot parser to support a fair target test without outcome-driven normalization.

Frozen support gates included:

- source identity understood;
- unchanged parser coverage >=60%;
- >=80 shared physical leaves;
- >=300 accepted tokens in each of five frozen folds;
- no pair/residual target metric computed during Stage A.

Observed:

- live source exactly matches historical Phase63B IT2a bytes;
- 99/99 #58C leaves overlap;
- 34,411 clean tokens;
- 28,280 accepted tokens;
- direct parser coverage `0.8218302287`;
- fold accepted counts `4976/5416/6261/6197/5430`;
- Stage-A scientific target metric flag false.

Status: **SUPPORTED — TARGET AUTHORIZED BEFORE SCIENCE**.

## H58D-EXIST — residual token-construction graph independently exists in IT2a

Prediction:

Under IT2a's own independently split line-local reference/test null calibration, pooled residual energy will exceed the independent test-null ensemble and reproduce across physical leaves.

Frozen gates:

- `p_exist_IT <= .01`;
- `W_IT_ALL >= .50`.

Observed:

- `E_IT_ALL = 3.2136322368`;
- test-null median `0.9925152695`;
- test-null q95 `1.1505067992`;
- test-null maximum `1.2589070830`;
- `p_exist_IT = 1/1001`;
- `W_IT_ALL = 0.9537725542`.

Status: **STRONGLY SUPPORTED**.

The #58C residual-existence result therefore survives an independent Takahashi reading lineage without reusing ZL3b's null calibration.

## H58D-STRATA — residual construction independently exists throughout planned IT2a strata

Prediction:

All source-grounded Currier/section/line-position groups retained from #58C will show supported residual existence under family-wise maxT control.

Observed:

All seven groups pass at `p_E,maxT = 1/1001`, with reliability `W = 0.8862–0.9417` and energies `2.9908–3.1773`.

Status: **SUPPORTED**.

## H58D-TOPOLOGY — complete residual graph replicates across ZL3b and IT2a

Prediction:

If IT2a residual existence passes, its complete 66-edge residual topology will agree with the already frozen ZL3b #58C graph beyond an independent IT2a line-local null, without selecting individual edges.

Frozen strong topology gates used both:

- complete-graph Pearson correlation;
- complete-graph sign agreement;
- maxT control across all eight planned groups.

Observed pooled `ALL`:

- Pearson `0.9884483853`;
- sign agreement `65/66`;
- `p_R,maxT = 1/1001`;
- `p_sign,maxT = 1/1001`.

Observed across all eight groups:

- Pearson range `0.97031–0.99548`;
- sign agreement range `64/66–66/66`;
- all eight groups: `CROSS_READING_TOPOLOGY_REPLICATED`;
- all correlation/sign maxT p-values `1/1001`.

Independent cross-reading null maxima:

- correlation max `0.43199`;
- sign agreement max `47/66`.

Status: **STRONGLY SUPPORTED**.

This is the principal evidence that the residual topology is not merely a ZL3b-specific reading artifact under the tested common representation.

## H58D-GEOMETRY — broad shared-core-plus-modulation geometry replicates

Frozen broad geometry requirement:

- residual existence in all seven target strata;
- no `DIFFERENT_RESIDUAL_OR_MIXTURE` contrast;
- >=6/7 within-IT contrasts stable or related/modulated.

Observed:

- all seven strata supported;
- all 7/7 contrasts stable or related/modulated;
- zero difference/multiple-grammar contrasts;
- same family-level results as #58C:
  - `REGISTER/SECTION RESIDUAL MODULATION`;
  - `LINE-POSITION RESIDUAL MODULATION`.

Status: **SUPPORTED**.

## H58D-MODORDER — exact stable-vs-modulated contrast ordering replicates

This was a secondary, finer geometry test frozen before reveal.

Observed:

- `G_core = 0.8369614`;
- `G_mod = 0.6821674`;
- `Delta_geometry = 0.1547940`;
- one-sided `p_Delta = 0.08791`.

Frozen gate required `Delta > 0` and `p_Delta <= .05`.

Status: **NOT SUPPORTED AT THE FROZEN THRESHOLD**.

The broad modulation geometry replicates, but the finer claim that the four historically stable contrasts are systematically more similar than the three historically modulated contrasts does not pass its secondary significance gate. Do not erase this retained non-pass.

## H58D-PARSER — primary replication is not qualitatively reversed by legal parser ambiguity

Non-promoting parser-`max` sensitivity:

- pooled E `3.13006`;
- pooled W `0.95242`;
- within-IT contrast correlations retain the same broad pattern.

Status: **QUALITATIVE ROBUSTNESS SUPPORTED; NON-PROMOTING**.

This does not eliminate parser/representation uncertainty, especially because 9,492 IT2a accepted tokens have different legal `min` and `max` parses.

## Frozen overall disposition

> **`INDEPENDENT TRANSCRIPTION REPLICATES RESIDUAL TOKEN-CONSTRUCTION CORE`**

The strongest retained statement is:

> **Within a common EVA/IVTFF framework, a manuscript-level token-internal construction signature survives independent ZL3b and Takahashi/IT2a readings. It contains a broad shared residual core with stratum modulation rather than an exactly uniform token grammar.**

## New follow-up hypothesis generated after #58D

The token-construction lane has now supplied a portable enough structural constraint to be used prospectively rather than merely described.

The next separately preregistered program should ask:

> **Can any bounded reversible/generative/inverse mechanism jointly reproduce the cross-reading residual token-construction core, accepted A1/H62 recurrence geometry, and the difficult signed S1 paragraph-entry specialization on held-out material without post-hoc repair and under explicit complexity accounting?**

The scientific objective now shifts from additional local structure discovery toward **mechanism discrimination and inversion**.

A separate GC2a/v101 alphabet-level robustness lane remains scientifically useful, but should not displace the higher-information inverse-model frontier unless a specific representation objection requires it.

## Interpretation boundary

Do not infer from #58D:

- that spaces are linguistic words;
- sentence grammar;
- semantic slot meanings;
- a plaintext alphabet or cipher table;
- a unique historical tokenization;
- a specific historical generator/cipher;
- semantic presence or absence;
- decipherment.

## Disposition summary

- Stage-A source/support adequacy: **SUPPORTED**;
- IT2a residual existence: **STRONGLY SUPPORTED**;
- residual existence across all planned IT2a strata: **SUPPORTED**;
- complete ZL3b↔IT2a residual topology: **STRONGLY SUPPORTED**;
- broad shared-core-plus-modulation geometry: **SUPPORTED**;
- exact stable-vs-modulated ordering: **NOT SUPPORTED AT FROZEN .05 GATE**;
- parser ambiguity robustness: **QUALITATIVE SUPPORT / NON-PROMOTING**;
- next frontier: **prospectively constrained reversible/inverse mechanism tournament**.