# Issue #68 — first-reveal transport failure record

Status: **SCIENTIFIC FIRST EXECUTION OCCURRED; RESULT TRANSPORT FAILED BEFORE ARCHIVE**

This record is committed **before any recovery execution**.

## 1. Scientific first-reveal event remains fixed

The preregistered first-reveal event was opening PR #71 from `issue68-joint-constraint-target` to `main`.

Exact first scientific execution:

- PR: `#71`
- workflow: `Issue68 R1 first reveal`
- workflow run: `33456282445`
- job: `99696811349`
- exact candidate head: `65e020cb2827a28670005da6d5d02bd6a6c1e51a`
- target scorer Git blob: `e94a24fbdfbb922099407313f23a1b87859130b6`
- first-reveal workflow Git blob: `ce57060cad2308e19ae580fa42f66fb6948e285d`
- design main: `b2298d7fe251070dacd21852ae3b5a1dac95fe65`

All exact-head chronology, archived-preflight, external-source, compilation and synthetic checks passed before the real target command.

## 2. Real scientific computation completed successfully

The workflow step:

`FIRST REVEAL — compute real Naibbe complete-66 R1 target`

completed with conclusion **success**.

The exact frozen scorer completed:

- all `1,000 / 1,000` reference-null realizations;
- the real Naibbe complete-66 R1 computation;
- all `1,000 / 1,000` independent test-null realizations;
- final result serialization command exited `0`.

The workflow log shows reference null progress through `1000/1000` and test null progress through `1000/1000` before the next step began.

Therefore the scientific first reveal **has happened**. It must not be replaced by relabeling a later run as the first reveal.

## 3. Failure occurred only at result framing / transport

The target command redirected stdout to:

`/tmp/issue68_joint_tournament_results.json`

The following validation step attempted `json.load(...)` on that file and failed with:

`JSONDecodeError: Expecting value: line 1 column 1 (char 0)`

The target scorer itself had already exited successfully.

The same project previously observed that imported legacy scientific helpers can print informational text to stdout before a final JSON object. The target workflow did not isolate that final JSON from legacy stdout before calling `json.load`.

Because validation failed:

- the packaging step was skipped;
- the upload-artifact step was skipped;
- the runner-local stdout/result file was not preserved after job termination.

## 4. No scientific target value was observed before recovery

The first-run GitHub log contains null-progress counters and the JSON framing error, but **does not expose**:

- real Naibbe Q values;
- residual-Z values;
- residual energy;
- reliability;
- ZL3b/IT2a correlations;
- sign agreements;
- R1 p-values;
- R1 pass/fail;
- candidate/global classification.

Thus no target result was available to drive a repair, threshold change, candidate change, seed/view change, or selective rerun.

## 5. Recovery is deterministic transport reconstruction, not a new scientific reveal

A recovery execution is authorized only to reconstruct the lost result bytes under the following immutable conditions:

1. checkout and execute the **exact original first-reveal head** `65e020cb2827a28670005da6d5d02bd6a6c1e51a`;
2. execute the exact `target68.py` blob `e94a24fbdfbb922099407313f23a1b87859130b6` without modification;
3. use the exact frozen CREMMA commit `292525969ad98380b398e6606a9c2a36d51913ae`;
4. use the exact frozen Naibbe commit `f2675ec5dd275268bc64dd48ea64fc0e0e9827a2`;
5. use the same first-run runtime package versions observed in run `33456282445`: `numpy 2.5.2`, `scipy 1.18.1`, `pandas 3.0.5`;
6. preserve `TARGET_HEAD_SHA=65e020cb2827a28670005da6d5d02bd6a6c1e51a`;
7. preserve every already frozen seed, null namespace, line order, parser policy, target reference and gate;
8. save the **entire raw stdout** before parsing;
9. isolate the final JSON object mechanically from that stdout without modifying its scientific content;
10. archive both raw stdout and isolated JSON with SHA-256 provenance.

No target scorer code, scientific parameter, threshold, candidate set, parser policy, representation gate, output view, seed, null namespace, or target reading may be modified for recovery.

## 6. Provenance semantics after recovery

The permanent record must distinguish:

- **scientific first execution**: run `33456282445`, job `99696811349`;
- **deterministic transport recovery execution**: later run/job, to be recorded after it occurs;
- **recovered scientific JSON**: deterministic reconstruction of the result generated by the first execution, not a claim that the recovery run was the original first reveal.

If deterministic recovery cannot reproduce a valid result under the exact frozen first-reveal code/input/runtime identities above, Issue #68 must be marked transport-inconclusive rather than repaired adaptively.
