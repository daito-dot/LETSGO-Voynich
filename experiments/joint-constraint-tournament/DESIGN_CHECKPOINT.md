# Issue #68 design checkpoint

Status: **DESIGN/AUTHORITY CHECKPOINT — READY FOR MAIN INTEGRATION**

This checkpoint contains no Issue #68 target result.

## Base authority

Issue #68 began from post-#58D main:

`a7a0f4bf9a0b8e5b0df665761702fe8881289d15`

#58D had already frozen:

`INDEPENDENT TRANSCRIPTION REPLICATES RESIDUAL TOKEN-CONSTRUCTION CORE`

with pooled ZL3b↔IT2a complete residual topology correlation `0.9884483852763541` and `65/66` sign agreement.

## Chronology firewall

- `STAGE0_PLAN.md` existed before Stage0 audit executable work.
- Stage0 audit/external-source checks occurred before any Issue #68 target plan/result.
- `STAGE0_PRETARGET_CORRECTIONS.md` records the Naibbe decoder/table-path and C0 reversibility corrections before target scoring.
- the exact Phase62C first-reveal artifact was recovered and permanently archived before the tournament target was implemented.
- `STAGE0_REPORT.md` and `stage0/STAGE0_OUTCOME.json` close Stage 0.
- `PLAN_A.md` was committed only after Stage 0 was complete.
- no real candidate 66-edge R1 graph, residual energy, target correlation/sign agreement, or joint tournament classification exists in this checkpoint.

## Stage0 frozen result

> `JOINT TOURNAMENT AUTHORITY READY`

Key qualifications retained:

1. Phase62P/H62 current semantic replay is exact.
2. Phase62C's exact first-reveal artifact differs from the later committed record at three non-primary fold2/Mazarine915 LOMO numeric fields.
3. protected Phase62C primary scientific field mismatches are zero, so the accepted Phase62C decision is unchanged.
4. exact Phase62C first-reveal bytes are now permanently archived under `experiments/phase62/first-reveal/`.
5. C0-4 is exactly structured-reversible without boundary side information.
6. pinned Naibbe contains `decrypt_naibbe.py`; fair decoder closure is unique exact recovery of the normalized plaintext letter stream, not original orthography/boundaries and not stochastic ciphertext reproduction.

## Frozen first-tournament responsibilities

`PLAN_A.md` fixes:

- **R1** complete-66 candidate-owned residual graph, compared to both ZL3b and IT2a with familywise protection over both prospectively selected R1 candidates and both target readings;
- **R2** unchanged H62-P1 recurrence geometry;
- **R3** unchanged signed S1 paragraph-entry specialization;
- **R4** source-side unique decoder closure for Naibbe;
- **R5** target-information/access firewall.

First R1 candidates are closed to:

- B2 published Naibbe C1-E0;
- B3 A1/A1-R1 surface generator.

N0 and C0-4 remain anchors, not R1 competitors.

## Required next implementation sequence

After this design checkpoint is merged to main:

1. create a fresh implementation branch from exact post-merge main;
2. implement candidate reconstruction/coverage and R4 closure first;
3. implement R1 complete-graph/null machinery only after plan ancestry is verified;
4. run preflight with all real-candidate R1 target quantities forbidden/skipped;
5. freeze exact implementation head;
6. trigger one explicit first reveal;
7. archive raw result bytes/hashes before interpretation;
8. update issue/research Source of Truth only from the frozen result.

No implementation commit may modify `PLAN_A.md` merely because a candidate fails.
