# Issue #75 Phase F — preregistration of the K/R/S-gated two-mode generator target test

Date: 2026-09-02  
Status: **PREREGISTERED AFTER F0 TRAINING-ONLY SELECTION / BEFORE PHASE-F EXECUTABLE / NO M6 TARGET RESULT**

## Scientific context

The minimum-generator ladder has established:

- M2 K/R/S coarse geometry: real but insufficient;
- M3 position-specific local adjacency: large gain but insufficient;
- M4 generic nonlocal distance coupling: small additional gain and insufficient;
- M5 two global hidden local-chain construction modes: large additional gain, `median T=0.7251534248`, but insufficient against the frozen empirical-signature M+ criterion.

Phase F0 then selected the next architecture without reading target R1 topology.

Frozen F0 authority:

- run `33545246568` — success;
- artifact ID `9817348700`;
- artifact digest `sha256:e56a2b4ab6e6741420c3ba3e430d16ec0c3a565abf4a40c7a917425da82cf7da`;
- authority SHA-256 `999d9990449875708019ad71aa3a1d253afad19edada88cb45eb4204349887c6`;
- permanent evidence commit `fd1446d474208b9e2f17e3fd4df5249e133c2bd3`;
- classification `F0_SELECT_KRS_GATED_TWO_MODE_CHAIN`.

F0 selected G2 because it improved exact held-out conditional occupancy likelihood over M5 in all five physical folds with median `+0.0600306734 nat/token`, while the larger global three-mode G3 family lost directly to G2 in four of five folds and had median `H_G3-H_G2=-0.0182096948 nat/token`.

No Phase-F target score exists at this plan.

## Primary Phase-F question

Can the complete independently replicated 66-edge residual occupancy topology be generated by **two local construction grammars whose mixing probability is controlled by coarse token geometry K/R/S**, without any explicit nonadjacent interaction or signature-specific parameter?

This is the direct target test of the architecture selected prospectively by Phase F0.

## Candidate family

Family name:

`M6-KRS-GATED-2MIX-CHAIN`

For each held-out physical fold:

1. draw a K/R/S descriptor `d=(K,R,S)` from the exact empirical descriptor distribution of the other four physical leaves;
2. standardize K/R/S using the exact outer-training means and population standard deviations frozen in the Phase-F0 authority;
3. compute

   `eta(d) = a0 + aK*zK(d) + aR*zR(d) + aS*zS(d)`;

4. draw hidden construction regime `Z` with

   `P(Z=1 | d)=logistic(eta(d))`;

5. conditional on Z and d, draw the complete 12-slot occupancy signature from that component's position-specific unary plus nearest-neighbour local-chain exponential family.

Each component has the same 21 free continuous parameters as M3/M5.

Nominal free parameters per fold:

- 21 component-0 parameters;
- 21 component-1 parameters;
- 4 geometry-gate parameters;
- total `46`.

There are exactly:

- latent global component count `2`;
- explicit nonadjacent interaction parameters `0`;
- generic distance parameters `0`;
- named distant-pair parameters `0`;
- complete-signature-specific parameters `0`;
- leaf-specific gate parameters `0`;
- line-specific gate parameters `0`.

The mode label has no semantic interpretation.

## Parameter authority — no Phase-F refitting

Phase F must use the already-frozen Phase-F0 selected G2 fit for each outer physical fold. **No Phase-F optimizer or start selection is permitted.**

The authoritative parameter source is:

`experiments/minimal-occupancy-generator/stage-f0/phase75f0_training_latent_diagnostic.json`

with exact SHA-256:

`999d9990449875708019ad71aa3a1d253afad19edada88cb45eb4204349887c6`.

For each fold, Phase F must read only the following frozen F0 fields:

- `descriptor_standardization.mean_K_R_S`;
- `descriptor_standardization.population_sd_K_R_S`;
- `descriptor_standardization.active_K_R_S`;
- `g2.gate_intercept_and_K_R_S_slopes`;
- `g2.selected_theta0`;
- `g2.selected_theta1`;
- `g2.selected_start_index` for provenance only.

Frozen selected G2 start indices are:

`[3, 6, 4, 7, 6]` for physical folds `0..4`.

All F0 held-out score fields become descriptive provenance only. They cannot alter Phase-F generation or target scoring.

## Descriptor distribution

The K/R/S descriptor probability `q_f(d)` for held-out fold `f` must be reconstructed exactly from the unchanged frozen ZL3b source and the unchanged five physical-fold partition, using only tokens from folds `!=f`.

The generator's full-state probability for state x in descriptor class d is

`P_f(x) = q_f(d) * [(1-g_f(d))*p_0(x|d) + g_f(d)*p_1(x|d)]`,

where `g_f(d)=P(Z=1|d)` from the frozen F0 gate.

The resulting probability over all 4095 non-empty 12-slot states must normalize to one within floating-point audit tolerance after explicit stable renormalization.

## Numerical evaluation

Reuse the Phase-F0 normalization-only stable component evaluator:

- class logits evaluated in float64;
- `logsumexp` within each K/R/S class;
- exponentiate normalized log probabilities;
- explicitly renormalize the floating probability vector within class;
- adjust log probabilities by the same normalization factor;
- no clipping of component probabilities, logits, gate probabilities, or parameters.

This is a deterministic evaluation of the already-frozen F0 parameters, not a fit.

## F1 target-blind generated authority

Before any Phase-F target topology access, generate exactly 31 complete candidate corpora for reps `0..30`.

For each rep and physical fold, sample exactly the frozen held-out token count from that fold's full 4095-state M6 probability vector.

Frozen sampling namespace:

`issue75:phaseF:M6-KRS-GATED-2MIX-CHAIN:rep{r}:fold{f}:generate`

Use the same deterministic `sample_prob` mechanism used in earlier Issue-75 phases.

The generated corpus must preserve the exact frozen overall/physical-fold token counts:

- total parsed tokens `25071`;
- fold counts `[4430,4810,5516,5447,4868]`.

For every rep record and freeze:

- exact occupancy SHA-256 of the full `25071 × 12` uint8 array;
- slot marginals;
- adjacent joint occupancies;
- generated K/R/S distribution;
- distinct signature count;
- all-zero count;
- target-access audit fields.

Requirements:

- exactly 31 unique generated occupancy corpora;
- no drops;
- no rerolls;
- no case replacement;
- pair-Q not computed;
- residual-Z not computed;
- ZL3b target R1 not loaded;
- IT2a target R1 not loaded;
- target correlation not computed;
- target sign agreement not computed;
- T not computed.

The complete F1 generated authority must be permanently frozen by SHA-256 before target access.

## Pretarget replay and candidate-owned-null validation

After F1 is frozen and before target reveal:

1. regenerate reps `0` and `30` from the frozen F0 parameter authority and source population;
2. require exact full occupancy SHA equality to the F1 authority;
3. run a rep-0 candidate-owned null smoke using the unchanged 1000-reference / 1000-test null configuration;
4. monkeypatch/hard-block the target-reference loader during the smoke so any target access aborts;
5. require a complete 66-dimensional residual vector, finite residual energy, reliability, and existence statistic;
6. freeze the Phase-F scorer and aggregate decision law before target access.

No target correlation or T may exist at the pretarget freeze.

## Frozen target measurement

After all pretarget gates pass, execute exactly one complete 31-case first reveal.

For each rep:

1. reconstruct/regenerate the candidate and require exact F1 occupancy SHA equality **before** target loading;
2. build the candidate-owned 1000-reference / 1000-test null exactly as in prior phases;
3. compute the complete 66-edge candidate residual vector;
4. only then load the frozen ZL3b and IT2a target residual references;
5. report Pearson correlation with all 66 target edges separately for ZL3b and IT2a;
6. report sign agreement separately;
7. compute

   `T_M6[r] = min(R_ZL3b[r], R_IT2a[r])`.

No edge subset, hand-picked residual, target-selected sign, or target-driven latent parameter is permitted.

## Frozen primary sufficiency decision

Reuse the exact Phase-A empirical-signature positive-control aggregate:

`fc3788c01bfa908bcae528a7a2606d508d26f694bf5ae51bc6cb537232efb540`

and exact paired q95 stochastic tolerance:

`delta_plus = 0.009768313008182594`.

For each rep define the same paired positive-control center used in Phases B–E and

`D_M6[r] = T_M6[r] - T_plus_center_PhaseA[r]`.

Primary quantity:

`gap_M6 = median_r D_M6[r]`.

If

`gap_M6 >= -0.009768313008182594`,

classify:

`M6_KRS_GATED_TWO_LATENT_CHAIN_MODES_SUFFICIENT`

and **STOP the R1 model-complexity ladder**. Do not target-test G3.

Otherwise classify:

`M6_KRS_GATED_TWO_LATENT_CHAIN_MODES_INSUFFICIENT_GLOBAL_THREE_MODE_FALLBACK_LICENSED`.

Only that frozen insufficiency outcome permits the already-prequalified `M6-GLOBAL-3MIX-CHAIN` family to proceed to a separately executed target phase.

## Secondary non-promoting diagnostics

Report, but do not use to override the primary decision:

- paired `T_M6-T_M5` across the same 31 reps;
- paired `T_M6-T_M4`;
- median ZL3b and IT2a sign agreement;
- residual energy, reliability W, and `p_exist`;
- q90/q99 M+ stochastic-tolerance sensitivity if retained identically from prior phases.

The expected scientific question is whether geometry-gating closes a substantial part of the remaining M5→M+ gap, not merely whether M6 is numerically greater than M5.

## Prequalified G3 fallback firewall

F0 also found `M6-GLOBAL-3MIX-CHAIN` predictively supported, but it was not selected because G2 was smaller and better in four of five held-out folds.

To eliminate post-target architecture shopping, the following sequence is frozen now:

- G2/M6 is tested first;
- if G2/M6 is sufficient, stop;
- if G2/M6 is insufficient, G3 may be tested exactly as already defined in F0, with no architecture, state-count, start, parameterization, or target-edge change based on Phase-F residuals;
- revealed Phase-F target residuals may not be used to create pair-specific interactions or modify G3.

## Stop conditions

Abort the Phase-F target reveal rather than repair after target access if any of these occurs:

- F0 authority SHA mismatch;
- source blob mismatch;
- parser/fold population mismatch;
- a frozen F0 parameter field is missing or non-finite;
- an M6 full-state distribution is non-finite or fails normalization;
- generated F1 occupancy SHA replay fails;
- any target-access flag becomes true before the pretarget freeze;
- scorer/aggregate authority changes after target access;
- any case is dropped or rerolled.

A pretarget mechanical failure may be repaired only prospectively and must be recorded before another target attempt.

## Interpretation boundaries

Even if M6 is sufficient, Phase F would establish a compact statistical generator of the replicated occupancy topology, not:

- meanings of the two latent modes;
- semantic word classes;
- plaintext words or letters;
- a cipher table;
- a natural-language morphology claim;
- a historical encoding procedure;
- Latin or another source language;
- decipherment.

The strongest licensed interpretation would be that the replicated within-token occupancy topology can be generated without explicit long-range pair terms by a compact hierarchy in which coarse token geometry gates a small set of local construction grammars.
