# Issue #81 prereveal amendment A

Timestamp context: committed while the first stage1 workflow was still running and before any stage1 target result or artifact was inspected.

Two clerical interpretations are fixed here without changing any fitted model, parameter grid, generator, target statistic, or numeric gate.

1. Issue #81 defines X1 as testing whether previous-token occupancy shape alone yields S2/H62. Therefore X1's standalone responsibility gate is the same S2 + raw-H62 + H62-profile conjunction used for X2. The sentence in `PLAN_A.md` saying X1 is "not licensed by an S1/S2/H62 target" means those targets are not available for fitting/tuning; it does not remove S2/H62 from X1's post-reveal responsibility classification. If X1 passes that conjunction but not the full S1+S2+H62+R1 recovery conjunction, its class is `PARTIAL`.
2. The stage1 implementation computes an algebraic X4 held-out likelihood diagnostic in the common cross-entropy function before the X4 generation license is known. That number is not used in `pi` selection, X2/X3 gates, X4 licensing, generation, or any recovery decision. If X4 is not licensed, the X4 cross-entropy field is non-authoritative and must be omitted/marked ignored in the final report. No X4 S1/S2/H62 generation is performed unless the frozen license fires.

The first workflow run, if it completes on the pre-amendment head, is not the final decision authority. The synchronized run containing this amendment is the authority.