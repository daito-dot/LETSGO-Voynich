# Issue #84 Phase B — prereveal notation amendment B0

Status: **FROZEN BEFORE `phase84b.py` EXISTS AND BEFORE ANY PHASE-B CANDIDATE OUTPUT IS SCORED**

Parent plan: `PLAN_B.md` at commit `b991b948f8bb4d5041ed2e6d348eef4d91feca84`.

## Clarification

The monoalphabetic substitution candidate named `S1` in `PLAN_B.md` is renamed **`SUB1`** in the executable and all result/report files.

Reason: `S1` is already the project-wide name for the paragraph-entry specialization statistic. Reusing `S1` for a cipher candidate would make later reports ambiguous.

This is a notation-only amendment. The candidate definition is unchanged: fixed one-to-one source-unit → opaque-atom substitution with word and line boundaries preserved and exact inverse decoding. Every reference in `PLAN_B.md` to the substitution candidate `S1` (including `Starting from S1`, `otherwise inverse S1`, and the S1/VERBOSE2 equality-relation self-test) shall be read as `SUB1`.

No candidate family, parameter, seed, statistic, target interval, decision rule, interpretation rule or source authority changes.
